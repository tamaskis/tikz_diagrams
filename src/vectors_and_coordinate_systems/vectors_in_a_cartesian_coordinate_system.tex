% ---------
% Preamble.
% ---------

% document type
\documentclass{article}

% import custom style
\usepackage{../.preamble/tikz_diagrams_template}

% color theme (black, red, blue, green, orange, purple, gold)
\colortheme{blue}

% ---------
% Document.
% ---------

\begin{document}

    % sets viewing orientation (declination/rotation) of 3D coordinate system
    \tdplotsetmaincoords{70}{115}

    % -----------------
    % TikZ environment.
    % -----------------

    \begin{tikzpicture}[tdplot_main_coords,scale=1]
        
        % -----------------------
        % Oxyz coordinate system.
        % -----------------------
        
        % origin
        \node[xshift=1,yshift=-6]at(0,0){$O$};
        
        % axes
        \draw(0,0,0)--(5,0,0)node[pos=1.07]{$x$};
        \draw(0,0,0)--(0,5,0)node[pos=1.03]{$y$};
        \draw(0,0,0)--(0,0,5)node[pos=1.035]{$z$};
        
        % basis vectors
        \draw[-mylatex',thick](0,0,0)--(1.5,0,0)node[above,xshift=-2]{$\mathbf{\hat{x}}$};
        \draw[-mylatex',thick](0,0,0)--(0,1.5,0)node[below,xshift=-15,yshift=3]{$\mathbf{\hat{y}}$};
        \draw[-mylatex',thick](0,0,0)--(0,0,1.5)node[right]{$\mathbf{\hat{z}}$};
        
        % -----------
        % Vector "a".
        % -----------
        
        % define components
        \pgfmathsetmacro{\ax}{2}
        \pgfmathsetmacro{\ay}{4}
        \pgfmathsetmacro{\az}{4}
        
        % vector
        \draw[-mylatex',thick,color_theme](0,0,0)--(\ax,\ay,\az)node[pos=0.75,above,yshift=2]{$\mathbf{a}$};
        
        % components
        \draw[dotted,color_theme](\ax,0,0)--(\ax,\ay,0)node[pos=0.5,below]{$a_{x}$};
        \draw[dotted,color_theme](0,\ay,0)--(\ax,\ay,0)node[pos=0.25,left,xshift=-1]{$a_{y}$};
        \draw[dotted,color_theme](\ax,\ay,0)--(\ax,\ay,\az)node[pos=0.5,right]{$a_{z}$};
        
        % -----------
        % Vector "b".
        % -----------
        
        % define components
        \pgfmathsetmacro{\bx}{4}
        \pgfmathsetmacro{\by}{4.5}
        \pgfmathsetmacro{\bz}{3}
        
        % vector
        \draw[-mylatex',thick,color_theme](0,0,0)--(\bx,\by,\bz)node[pos=0.75,above]{$\mathbf{b}$};
        
        % components
        \draw[dotted,color_theme](\bx,0,0)--(\bx,\by,0)node[pos=0.5,below]{$b_{x}$};
        \draw[dotted,color_theme](0,\by,0)--(\bx,\by,0)node[pos=0.6,right,xshift=6]{$b_{y}$};
        \draw[dotted,color_theme](\bx,\by,0)--(\bx,\by,\bz)node[pos=0.75,left,xshift=1]{$b_{z}$};
        
    \end{tikzpicture}

\end{document}